#pragma once

#include <mutex>
#include <dequeue>

namespace Network {




    template <typenmae T>
    class tsqueue{
        public:
            tsqueue() = default;
            tsqueue(const tsqueue<T>&) = delete;

        protected:

    }


};